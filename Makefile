SCRIPT_PATH = ./setup

install-app:
	${SCRIPT_PATH}/app

install-shell:
	${SCRIPT_PATH}/shell

install-dev:
	${SCRIPT_PATH}/dev

install-npm:
	${SCRIPT_PATH}/npm

install-nvim:
	${SCRIPT_PATH}/nvim

install-tmux:
	${SCRIPT_PATH}/tmux